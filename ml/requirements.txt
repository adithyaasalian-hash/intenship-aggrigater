# Kept deliberately small. Everything here has to fit, load and run inside a
# 512 MB free-tier container alongside the model.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
numpy==2.2.1
pyyaml==6.0.2
pymongo==4.10.1
pdfplumber==0.11.5

# ONNX-based embeddings: same all-MiniLM-L6-v2 weights as sentence-transformers
# but ~150 MB instead of ~1.2 GB, because it does not drag PyTorch along.
# Swap for sentence-transformers only if you have >1 GB of RAM to spare.
fastembed==0.5.1

# Optional second opinion on PDFs that pdfplumber cannot read. Comment out to
# save ~40 MB; extract_text() handles its absence.
PyMuPDF==1.25.2
Pillow==11.1.0
pytesseract==0.3.13
rapidocr_onnxruntime==1.2.3
